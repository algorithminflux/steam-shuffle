<Window x:Class="SteamShuffle.Views.AddGameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Game"
        Width="440" Height="520"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Add Game Manually" FontSize="18" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,4" />
        <TextBlock Text="For games your Steam Web API key can't see (e.g. Family Share)." Grid.Row="1"
                   Foreground="{StaticResource MutedTextBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,12" />

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Grid.Column="0" Margin="0,0,8,0" Tag="Search by game name..." KeyDown="SearchBox_KeyDown" />
            <Button x:Name="SearchButton" Grid.Column="1" Content="Search" Style="{StaticResource SecondaryButton}" Padding="10,6" Click="Search_Click" />
        </Grid>

        <ListBox x:Name="ResultsList" Grid.Row="3" Background="{StaticResource PanelBrush}"
                 BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                 DisplayMemberPath="Name" SelectionChanged="ResultsList_SelectionChanged" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <TextBlock x:Name="StatusText" Foreground="{StaticResource MutedTextBrush}" FontSize="11"
                       VerticalAlignment="Center" Margin="0,0,16,0" />
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="Cancel_Click" />
            <Button x:Name="AddButton" Content="Add" Style="{StaticResource PrimaryButton}" IsEnabled="False" Click="Add_Click" />
        </StackPanel>
    </Grid>
</Window>
